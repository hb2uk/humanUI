export const APP_NAME = 'HumanUI';
export const APP_VERSION = '0.0.0';

export const API_ENDPOINTS = {
  ITEMS: '/api/items',
  USERS: '/api/users',
} as const;

export const ROUTES = {
  HOME: '/',
  ADMIN: '/admin',
  API: '/api',
} as const; 