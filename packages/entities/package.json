{
  "name": "@humanui/entities",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@humanui/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
} 